# 🚀 Gangchill Full-Stack cPanel লাইভ ডেপ্লয়মেন্ট গাইড (Deployment Guide)

আপনার গ্যাংচিল (Gangchill B2B Hub) প্রজেক্টটি এখন সম্পূর্ণ ফুল-স্ট্যাক আর্কিটেকচারে রূপান্তরিত করা হয়েছে। এতে রয়েছে **React + TypeScript প্রোডাকশন ফ্রন্টএন্ড** এবং **PHP 8 + MySQL রিয়েল ব্যাকএন্ড রেস্ট এপিআই (REST API)**। 

সবকিছু প্রস্তুত করে একটি সিঙ্গেল রেডিমেড জিপ ফাইলে প্যাকেজ করা হয়েছে:
📁 **`gangchill-cpanel-ready.zip`** (ফাইলের আকার: ~৫.৫৮ MB)

---

## 🛠️ cPanel-এ লাইভ করার সহজ ধাপসমূহ (Step-by-Step Instructions)

### ধাপ ১: cPanel ফাইল ম্যানেজারে জিপ ফাইল আপলোড
1. আপনার cPanel-এ লগইন করুন।
2. **File Manager** ওপেন করে **`public_html`** ফোল্ডারে প্রবেশ করুন। (যদি সাব-ডোমেইনে হোস্ট করতে চান, তবে সেই সাব-ডোমেইনের রুট ফোল্ডারে যান)।
3. পূর্বে কোনো অপ্রয়োজনীয় ফাইল থাকলে সরিয়ে নিন বা ব্যাকআপ রাখুন।
4. ওপরের মেনু থেকে **Upload** বাটনে ক্লিক করে **`gangchill-cpanel-ready.zip`** ফাইলটি আপলোড করুন।
5. আপলোড সম্পন্ন হলে ফাইলে রাইট-ক্লিক করে **Extract** করুন সরাসরি `public_html`-এ।

---

### ধাপ ২: MySQL ডেটাবেজ ও ইউজার তৈরি
1. cPanel ড্যাশবোর্ডে গিয়ে **MySQL Database Wizard** ওপেন করুন।
2. একটি নতুন ডেটাবেজ নাম দিন (যেমন: `cpaneluser_gangchill`).
3. একটি নতুন ডেটাবেজ ইউজারনেম ও শক্তিশালী পাসওয়ার্ড দিন (যেমন: `cpaneluser_gangadmin` এবং আপনার পাসওয়ার্ড)।
4. **All Privileges** সিলেক্ট করে ইউজার ও ডেটাবেজ সংযোগ সম্পন্ন করুন।
5. ডেটাবেজ নাম, ইউজারনেম এবং পাসওয়ার্ডটি সংরক্ষণ করে রাখুন।

---

### ধাপ ৩: ডেটাবেজ স্কিমা ও সীড ডেটা ইমপোর্ট
1. cPanel থেকে **phpMyAdmin** ওপেন করুন।
2. বাঁপাশের মেনু থেকে আপনার সদ্য তৈরি করা ডেটাবেজটি সিলেক্ট করুন।
3. ওপরের **Import** ট্যাবে ক্লিক করুন।
4. **Choose File**-এ ক্লিক করে আপনার প্রজেক্টের `api/database/schema.sql` ফাইলটি নির্বাচন করুন এবং নিচে স্ক্রোল করে **Import** বাটনে ক্লিক করুন।
5. সাথে সাথে সবকটি ১৪টি টেবিল (`admins`, `stocks`, `investments`, `buyer_orders`, ইত্যাদি) তৈরি ও কনফিগার হয়ে যাবে।

> 🛡️ **নিরাপত্তা আপডেট (Security Hardening):** সাইবার নিরাপত্তার স্বার্থে `init.php` ফাইলটি প্রোডাকশন জিপ প্যাকেজ থেকে স্বয়ংক্রিয়ভাবে অপসারণ করা হয়েছে, যেন বহিরাগত কেউ ব্রাউজার দিয়ে ডেটাবেজ রান বা ওভাররাইট করতে না পারে। ডেটাবেজের সমস্ত টেবিল `schema.sql` ইমপোর্টের মাধ্যমেই সম্পূর্ণ তৈরি হয়ে যায়।

---

### ধাপ ৪: ডেটাবেজ কনফিগারেশন আপডেট
1. cPanel ফাইল ম্যানেজারে গিয়ে `api/config/database.php` ফাইলটি এডিট (Edit) করুন।
2. লাইন ১১ থেকে ১৫-তে আপনার cPanel ডেটাবেজের সঠিক তথ্য বসান:

```php
$host = getenv('DB_HOST') ?: 'localhost';
$port = getenv('DB_PORT') ?: '3306';
$db   = getenv('DB_NAME') ?: 'cpaneluser_gangchill'; // আপনার cPanel ডাটাবেজ নাম
$user = getenv('DB_USER') ?: 'cpaneluser_gangadmin'; // আপনার ডাটাবেজ ইউজার
$pass = getenv('DB_PASS') !== false ? getenv('DB_PASS') : 'your_db_password'; // আপনার ডাটাবেজ পাসওয়ার্ড
```
3. ফাইলটি **Save Changes** করুন।

---

### ধাপ ৫: ফাইল ও ফোল্ডার পারমিশন নিশ্চিত করা
1. নিশ্চিত করুন `api/uploads/` ফোল্ডারের পারমিশন **`755`** দেওয়া আছে, যাতে অ্যাডমিন ও ভিজিটরদের আপলোড করা ছবি সেখানে নিরাপদে সেভ হতে পারে।
2. `api/uploads/.htaccess` ফাইলটি স্ক্রিপ্ট এক্সিকিউশন ব্লক করে আপলোড ফোল্ডারকে সম্পূর্ণ সুরক্ষিত রাখবে।

---

## 🔑 ডিফল্ট সুপারঅ্যাডমিন ক্রেডেনশিয়াল (Default Superadmin Login)

* **অ্যাডমিন প্যানেল URL:** `https://yourdomain.com/admin/login`
* **ইমেইল:** `admin@gangchill.com`
* **পাসওয়ার্ড:** `admin123`

> ⚠️ **জরুরি নিরাপত্তা নির্দেশনা (Security Action Required):** লাইভ সার্ভারে প্রথমবার লগইন করার সাথে সাথে অ্যাডমিন সেটিংস (`/admin/settings`) থেকে আপনার ইমেইল ও পাসওয়ার্ড পরিবর্তন করে একটি ইউনিক এবং শক্তিশালী পাসওয়ার্ড সেট করুন। ডিফল্ট ক্রেডেনশিয়াল কোনো অবস্থাতেই পাবলিক হোস্টিংয়ে রেখে দেবেন না।

---

## ✅ ভেরিফিকেশন চেকলিস্ট (Live Feature Verification)

| ফিচার | কার্যকারিতা | স্ট্যাটাস |
| :--- | :--- | :---: |
| **লাইভ ফিশ ক্যাটালগ** | ডাটাবেজ থেকে রিয়েল-টাইম বাংলা নাম, দাম ও স্টক লোড হয় | প্রস্তুত ✅ |
| **করপোরেট চাহিদা জমা** | হোম ও বাই পেজের ফর্ম থেকে সরাসরি MySQL `buyer_orders`-এ সেভ হয় | প্রস্তুত ✅ |
| **ঘাট লট সাবমিশন** | সেল পেজ থেকে চাষী/ঘাটের মাছের লট `seller_lots` টেবিলে জমা হয় | প্রস্তুত ✅ |
| **লট থেকে স্টক কনভার্ট** | অ্যাডমিন যাচাই শেষে ১-ক্লিকে লটকে লাইভ স্টকে রূপান্তর করতে পারেন | প্রস্তুত ✅ |
| **অর্ডার ও কোটেশন আপডেট** | অ্যাডমিন প্যানেল থেকে পারচেজ অর্ডার কোটেশন রেট নির্ধারণ ও স্ট্যাটাস বদল | প্রস্তুত ✅ |
| **রিয়েল মিডিয়া আপলোড** | JPG, PNG, WEBP ফাইল ভ্যালিডেশন সহ সার্ভারের `uploads/`-এ জমা হয় | প্রস্তুত ✅ |
| **রক্ষণাবেক্ষণ মোড (Maintenance)** | সেটিংস থেকে মোড অন করলে পাবলিক চাহিদা জমা স্বয়ংক্রিয়ভাবে স্থগিত থাকে | প্রস্তুত ✅ |
| **রেস্পন্সিভ ও কমপ্যাক্ট UI** | মোবাইল, ট্যাবলেট ও ডেস্কটপে আরামদায়ক ও নিখুঁত অভিজ্ঞতা | প্রস্তুত ✅ |

---

**অভিনন্দন! আপনার Gangchill ফুল-স্ট্যাক ওয়েবসাইট ও অ্যাডমিন হাব cPanel-এ লাইভ হওয়ার জন্য ১০০% প্রস্তুত।**
